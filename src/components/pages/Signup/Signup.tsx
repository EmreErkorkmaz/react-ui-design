import styles from '../../../App.module.scss';

const Signup = (props: any) => {
    return (
        <h1 className={styles["sign-up"]}>
            Sign Up
        </h1>
    )
}

export default Signup
